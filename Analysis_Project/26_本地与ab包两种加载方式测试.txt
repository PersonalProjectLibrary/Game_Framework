原先热更测试都是有热更检测下载，但是是本地资源加载，不是从AB包里加载。
热更加载后看不到热更效果。

这里重新打包下（非热更打包），
然后把Asset同级目录下：AssetBundle\StandaloneWindows64里的ab包，
复制到Asset文件夹下的StreamingAssets文件夹里。
然后把ResourceManager.cs里的变量m_LoadFormAssetBundle 从false改为true；
后面运行，变为从ab包加载，而不是本地加载。

这时候运行，热更效果（本地模型脸是原脸，热更里模型脸被涂抹过）就加载显示了。

这里可删除SeverInfo.xml里的此次patchs，实现热更回退。