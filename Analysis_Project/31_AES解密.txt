继续完善AES.cs里功能，

1、对AESFileDecrypt(...)方法测试，测试能否正常解密
注：该方法解密过程中，实际对文件进行了重写操作，就像注释写到的，不使用运行时使用。

（1）解密对象：同对AESFileEncrypt(...)加密方法测试，
对之前加密的Assets/GameDatas/Data/Xml文件夹里的AESFileEncrptyData.xml文件进行解密。

（2）添加解密测试函数：
在BundleEditor.cs里添加测试加密方法；写的方法放到顶部工具栏 测试 下
[MenuItem("测试/测试AES解密")]
void TestAESDec(){...}

（3）修改加密方法在菜单栏上的命名：
对BundleEditor.cs里添加测试加密方法在菜单栏上的显示名修改
[MenuItem("测试/测试AES解密")]
void TestAESEnc(){...}
改为
[MenuItem("测试/测试AES加密")]
void TestAESEnc(){...}

（4）点击顶部工具栏 测试 下的 测试AES解密 按钮，
再选中AESFileEncrptyData.xml，可以发现AESFileEncrptyData.xml解密回可读文件。

（5）为了规范，这里将AESFileEncrptyData.xml改名为TestAESData.xml
对应的BundleEditor.cs的TestAESEnc()和TestAESDec()两个方法里FilePath参数也修改为：
AES.AESFileEncrypt(Application.dataPath + "/GameData/Data/Xml/TestAESData.xml", "Ocean");
AES.AESFileDecrypt(Application.dataPath + "/GameData/Data/Xml/TestAESData.xml", "Ocean");